# BTHaven integration tests

These tests require a Windows 11 machine, a Bluetooth adapter, and explicitly selected physical devices. They are not part of the default unit-test pass and must redact identifiers in any published result.
