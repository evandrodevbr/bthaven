# BTHaven.Windows

Windows-specific adapters for DeviceWatcher, WinRT Bluetooth, AudioPlaybackConnection, Core Audio/WASAPI, tray integration, and future call transport. Keep public Windows types behind the `BTHaven.Core` contracts.
